import {Request, Response} from "express";

// return all books as json
export const getAllBooks = async (requests : Request, res : Response) => {}

// save book to the database
export const addPost = async (requests : Request, res : Response) => {}
